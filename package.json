{
  "name": "e2e-cypress",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "tests": "mocha cypress/integration/**/*.js",
    "cy:pipeline": "start-server-and-test up http-get://localhost:8080 cy:run",
    "cy:run": "cypress run --headless --record",
    "up": "lerna run start --parallel"
    "start": "cd todomvc; npm start -- --quiet",
    "test": "cypress run --spec 'cypress-environment/cypress/integration/*'",
    "ci": "start-test 3000"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^7.5.0",
    "cypress-iframe": "^1.0.1",
    "cypress-xpath": "^2.0.1"
  }
}
