{
  "name": "e2e-cypress",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "cypress run --spec 'cypress/integration/*.js'",
    "start_chrome": "cypress run --spec 'cypress/integration/*.js' --browser chrome",
    "start_electron": "cypress run --spec 'cypress/integration/*.js' --browser electron",
    "start_firefox": "cypress run --spec 'cypress/integration/*.js' --browser firefox"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^7.5.0",
    "cypress-iframe": "^1.0.1",
    "cypress-xpath": "^2.0.1"
  }
}
